1. Проверка перепутанных ордеров в функции orders_processing(). Код нужно добавить в самом конце после if clOrdID in var.orders:

            n = Function.order_number(self, clOrdID)
            size = orders.listboxes[-1].size()
            for num in range(size):
                emi = orders.listboxes[-1].get(num)
                print(emi)
                if emi == var.orders[clOrdID]["EMI"]:
                    print("_________________debug", num, n + orders.mod)
                    if num != n + orders.mod:
                        with open("debug/debug.txt", "a") as f:
                            f.write("var.orders and orders table not are synchronized, clOrdID:" + clOrdID + ", leavesQty:" + var.orders[clOrdID]["leavesQty"] + ", execType:" + row["execType"])
                    break